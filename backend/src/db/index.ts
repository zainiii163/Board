// PostgreSQL connection — will use pg or drizzle-orm.
// Placeholder until database is configured.

export async function connectDb() {
  console.log("[db] PostgreSQL connection placeholder — not yet configured.");
}
